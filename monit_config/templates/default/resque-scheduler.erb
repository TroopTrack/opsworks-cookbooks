check process <%= @app_anme %>
  with pidfile "<%= @pid_file %>"
  group <%= @resque_group_name %>
  start program = "/usr/sbin/service <%= @app_anme %> start"
  stop program = "/usr/sbin/service <%= @app_anme %> stop"
  if 5 restarts within 5 cycles then timeout